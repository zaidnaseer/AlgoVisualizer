import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} DSA Visualization App</p>
        </footer>
    );
}

export default Footer;
